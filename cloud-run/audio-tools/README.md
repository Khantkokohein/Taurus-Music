# Taurus Audio Tools

Cloud Run FFmpeg service for Studio Editor operations:

- crop
- fade in/out
- split
- export MP3/WAV
- selected range export

The public app should call this through `/api/audio-edit`, not directly.
